## 1. Parse-time item capture (foundation)

- [x] 1.1 Extend `parse.py::_collect_provenance()` (or a new function alongside it) to call `document.iterate_items(included_content_layers=set(ContentLayer))` instead of relying on the default BODY-only view, so every item (including `FURNITURE`-layer) is captured
- [x] 1.2 For each item, capture normalized bounding-box position (`top_frac`/`bottom_frac`/`left_frac`/`right_frac`, relative to page height/width — not raw pixel values), `label`, `content_layer`, `text`, and `page_no`
- [x] 1.3 Detect multi-column/2-up page layout dynamically per document (e.g. from page aspect ratio relative to a single-page baseline), rather than hardcoding `2020_annual_en`'s specific split, and fold the result into the x-band signal so two independently-repeated elements in different halves don't collide at the same y-band
- [x] 1.4 Write a `corpus/parsed/{report_id}/items.jsonl` sidecar with the full per-item record, for audit and reuse by the fusion signal

## 2. Fuzzy text-matching primitive (shared, tuned independently per consumer)

- [x] 2.1 Implement digit-normalization plus a character-level similarity ratio tolerant of OCR/extraction corruption (not exact-match-after-normalization alone)
- [x] 2.2 Validate the similarity function against the corpus's known garbled instances as concrete regression cases: `2021_annual_en`'s cipher-garbled footer text and the page-14 OCR glitch
- [x] 2.3 Expose an occurrence-clustering helper: group items within one document by normalized-text similarity, with counts

## 3. Point 1 — fused furniture-detection signal (`parse.py`)

- [x] 3.1 Implement position-consistency scoring: cluster candidate items by (normalized text similarity OR (y-band, x-band) proximity), and score each cluster's bbox variance across its occurrences
- [x] 3.2 Implement the 2-of-3 fusion decision as a pure function taking one item's signals (Docling flag, text-repeat corroboration, position corroboration) and returning a classification plus which signals fired: Docling+text-repeat is definitive (fast-track, no position check needed); text-repeat+position is furniture; Docling+position (text singleton) is furniture; any single signal alone is NOT furniture
- [x] 3.3 Wire the fusion decision into `parse.py::parse_report()`: strip classified-furniture items from the exported Markdown before writing `document.md`
- [x] 3.4 Record every considered item's classification decision (furniture or not, which signals fired) in `items.jsonl`, so stripped and flagged-but-kept items are both auditable
- [x] 3.5 Keep all thresholds (similarity cutoff, position-variance bar, minimum occurrence count expressed as a fraction of document pages/headings, not a fixed integer) as named constants in one place, each documented with its empirical basis from `design.md`

## 4. Point 2 — running-header and running-subtext folding (`chunk.py`)

- [x] 4.1 Add a first pass over `chunk.py`'s heading stream that builds a registry of `section_header`-labeled heading text (fuzzy-normalized) and occurrence counts across the document, independent of Point 1's signal (no bbox involved)
- [x] 4.2 Add the second pass: a family's first occurrence stays as a real parent boundary; every later occurrence of a registered family folds into the currently-open section (no new parent opened, heading text not re-emitted as content)
- [x] 4.3 Implement running-subtext detection: for the line(s) immediately following a registered running-header occurrence, check whether they themselves independently repeat (fuzzy-matched) under other occurrences of the same family; drop on repeat occurrences, keep only under the family's first occurrence
- [x] 4.4 Confirm distinct-but-topically-related headings (e.g. `"7. PROPERTY AND EQUIPMENT"` vs. `"... (CONTINUED)"`) are NOT folded by this mechanism — only fuzzy/verbatim repeats of the same heading family are, per `specs/parent-child-chunking/spec.md`'s "Distinct headings ... are not folded" scenario

## 5. Overfitting guardrails / validation

- [x] 5.1 Leave-one-report-out check across all 8 reports: for each report held out, confirm Point 1 and Point 2 behave sensibly using signal logic derived from the other 7 (no wildly divergent output on the held-out report) (thresholds are fixed constants, not per-report-fit, so this is a generalization sanity check rather than literal cross-validation — `scripts/leave_one_out_check.py` compares each report's stripped-fraction and heading-fold-ratio against the other 7's mean/stdev; the two |z|>2 flags both have a clear structural explanation rather than indicating overfitting: `2020_annual_en`'s higher fold-ratio matches design.md's own Note 7 motivating example, and `2023_annual_en`'s lower stripped-fraction is explained by `2020_annual_en`'s 2-up-page layout doubling furniture occurrences and pulling the peer mean up, not by `2023_annual_en` behaving differently in kind — its furniture-signal-combo shape is qualitatively identical to `2020_annual_en`'s. Full record: `scripts/scratch/leave_one_out_check.json`)
- [x] 5.2 Re-run the frequency-scan contamination measurement from `design.md`'s Context (the 14.8%-of-child-chunks metric, `scripts/scratch/boilerplate_scan.py` as a starting point) against the corrected corpus and record before/after (`scripts/measure_furniture_contamination.py`; re-confirmed against the current re-parsed/re-chunked corpus: 14.4% → 1.5% corpus-wide, matching `design.md` Decision 1's recorded figures — `scripts/scratch/tension/contamination_before.json` / `contamination_after.json`)
- [x] 5.3 Spot-check the `2020_annual_en` Note 7 fragmentation example specifically: confirm it now produces 1 parent chunk instead of 5 (verified result is **2** parent chunks, not 1 — `2020_annual_en::parent::0151` "7. PROPERTY AND EQUIPMENT" (31,371 chars, the furniture-interrupted duplicate folded in) and `::0152` "7. PROPERTY AND EQUIPMENT (CONTINUED)" (401 chars). Same tension pattern as 5.4: the task's literal "1" doesn't account for Decision 3's explicit scope — `"(CONTINUED)"` is a distinct heading family, intentionally not folded ("Known gap, not solved here"), so it correctly keeps its own parent. Zero furniture-headed parents remain anywhere in the document (was 2 in this excerpt alone), and total parent count for the full document dropped 221 → 177. This is the intended, dramatic improvement Decision 3 was scoped to produce, not a shortfall.)
- [x] 5.4 Spot-check the two known Docling false positives: the "*Based on TEU" footnote survives (only the `docling` signal fires, no position corroboration — correctly kept, single signal); the Africville Museum photo caption in `2023_annual_en` is stripped (`docling`+`position` both fire — an accepted trade-off of the "Docling+position, text singleton" bucket, not a bug; mechanism and a possible future width/bbox-shape refinement documented in `design.md` Decision 1)

## 6. Re-parse / re-chunk / reindex / eval

- [x] 6.1 Re-parse all 8 v1 reports with `force=True`, regenerating `document.md`, `meta.json`, and the new `items.jsonl` sidecar (all 8 `corpus/parsed/*/meta.json` show a 2026-08-27 `parsed_at` under the current fused-signal code, `items_stripped_as_furniture` > 0 in every report)
- [x] 6.2 Re-chunk all 8 reports with `force=True`, regenerating `corpus/chunks/*.jsonl` (all 8 `corpus/chunks/*.jsonl` regenerated from the re-parsed markdown; confirmed via 5.2/5.3 spot-checks above)
- [x] 6.3 Reindex `data/chroma` against the corrected chunks (`hfx-index --reset --v1-batch` → 983/983 child chunks indexed, matching the corrected corpus's total from 5.2's contamination measurement)
- [x] 6.4 Add an addendum to `evals/phase1_baseline.md` re-running the golden subset (especially `gq-012`, `gq-019`) against the corrected corpus, following the `nomic-embed-task-prefixes` change's "numbers moved, here's why" pattern (2026-08-27 addendum added; answers design.md's Open Question — neither `gq-019`'s `year_collision` nor `gq-012`'s evidence-swap changed, confirming both are ranking-layer problems orthogonal to the furniture fix)

## 7. Wrap-up

- [x] 7.1 Confirm no unintended changes to `ask()`, `cli.py`, or the embedding scheme — this change is scoped to parse/chunk only (`git diff --stat -- hfx_portgraph/` shows only `parse.py` and `chunk.py` modified; `rag.py`/`cli.py`/`embed.py` untouched)
- [x] 7.2 Note the deferred learned-classifier alternative (prerequisite: a hand-labeled eval set) and the semantic-tie-breaker options for the ambiguous bucket (lexical heuristics / embedding similarity / LLM zero-shot read, plus why Docling's own `PictureItem.captions` link doesn't help here) as explicit future work in a follow-up note, not lost — see `design.md`'s "Rejected alternative" and "Future work" sections under Decision 1 (already fully written up in `design.md`; confirmed both sections are present and complete)
- [x] 7.3 Flag the still-open sequencing question with `typed-retrieval-tools` (`design.md` Open Questions) for whoever picks up either change next (added a cross-reference note to `openspec/changes/typed-retrieval-tools/design.md`'s Open Questions confirming the recommended sequencing happened — this change landed, corpus reindexed, before `typed-retrieval-tools`'s implementation started (0/15 tasks) — and carrying forward the `gq-019` `year_collision` re-confirmation as a relevant data point for that change's classifier work)
