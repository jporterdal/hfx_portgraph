# Typed retrieval tools -- validation (tasks 3.3, 4.1-4.4)

Direct-call validation of `hfx_portgraph/retrieval_tools.py` against `evals/golden.jsonl`'s `expected_evidence` -- no LLM, no `ask()`. Generated by `scripts/validate_typed_retrieval.py` (n_results=6 per call). Full machine-readable output: `scripts/scratch/typed_retrieval_validation.json`.

## Task 3.3 -- section classifier spot-check

Ran `classify_section()` over all 983 child chunks' headings across the 8 v1 reports (not just gq-019's specific headings).

| class | count |
|---|---|
| statement | 46 |
| note | 188 |
| other | 749 |

Probe headings (chosen because they're the ones the failure modes in `evals/phase1_baseline.md` and this change's design.md turn on):

| heading | classification |
|---|---|
| 17. Comparative figures | note |
| 7. PROPERTY AND EQUIPMENT | note |
| Halifax Port Authority Consolidated statement of earnings | statement |
| Halifax Port Authority Consolidated statement of comprehensive income | statement |
| Halifax Port Authority Consolidated statement of changes in equity of Canada | statement |
| Halifax Port Authority Consolidated statement of financial position | statement |
| Statement of compliance | other |
| Inflation risk | other |
| Credit risk | other |

**Known limitation (under-triggering, not over-triggering):** note/footnote *subsections* that don't repeat their parent note's number in their own heading -- e.g. `Inflation risk`, `Credit risk`, `Pension benefits` (children of numbered notes like risk-management or employee-benefit disclosures) -- classify as `other`, not `note`. This is safe for the gq-019/gq-020 failure mode specifically (those collisions are against numbered top-level note headings like `17. Comparative figures`, which the classifier does catch), but means the classifier's `note` recall is incomplete for sub-item headings in general. Documented here per tasks.md 3.3/4.4 rather than tuned further, since it doesn't affect any tagged golden item's outcome below.

**Fixed during spot-check:** an earlier bare `\bstatement of\b` pattern misclassified `Statement of compliance` (an accounting-policy note, 7 occurrences across the corpus) as `statement`. The shipped classifier matches only canonical primary-statement titles (`statement of earnings`, `... comprehensive income`, `... financial position`, `... changes in equity`, `... cash flows`, `... operations`), which avoids that false positive while still matching every real primary-statement heading found in the corpus (see probe table above).

## Tasks 4.1-4.3 -- golden-set evidence coverage

12 golden items carry `multi_hop`/`yoy_metric`/`year_collision` tags.

### Multi-year / multi-report items (`multi_hop`, `yoy_metric`)

| id | tags | requested years | year coverage | requested report_ids | report_id coverage |
|---|---|---|---|---|---|
| gq-001 | multi_hop, narrative, table_heavy, yoy_metric | [2020, 2021, 2022, 2023] | 4/4 | 7 report(s) | 7/7 |
| gq-004 | multi_hop, table_heavy, yoy_metric | [2021, 2022] | 2/2 | 2 report(s) | 2/2 |
| gq-007 | multi_hop, narrative, yoy_metric | [2022, 2023] | 2/2 | 4 report(s) | 4/4 |
| gq-010 | multi_hop, table_heavy, yoy_metric | [2020, 2021] | 2/2 | 2 report(s) | 2/2 |
| gq-012 | multi_hop, narrative | [2021, 2023] | 2/2 | 2 report(s) | 2/2 |
| gq-014 | multi_hop, narrative, table_heavy | [2021] | 1/1 | 2 report(s) | 2/2 |
| gq-017 | multi_hop, table_heavy, yoy_metric | [2020, 2021, 2022] | 3/3 | 3 report(s) | 3/3 |
| gq-022 | multi_hop, narrative | [2021, 2022, 2023] | 3/3 | 3 report(s) | 3/3 |
| gq-024 | multi_hop, narrative, single_doc, table_heavy | [2022] | 1/1 | 1 report(s) | 1/1 |
| gq-025 | multi_hop, table_heavy, yoy_metric | [2020, 2023] | 2/2 | 2 report(s) | 2/2 |

**Summary:** 10/10 items got full requested-year coverage via `retrieve_by_year`'s per-year fan-out; 10/10 items got full requested-report_id coverage via `retrieve_by_report`'s per-report_id fan-out (its default behavior as of design.md Decision 7 -- see the comparison below for what the previous pooled-query default would have gotten instead). Compare gq-001 specifically against `evals/phase1_baseline.md`, where naive `retrieve()` missed 2023 entirely -- the per-year fan-out result for gq-001 is reported in the table above.

### Report-axis fan-out vs. pooled comparison (Decision 7)

`retrieve_by_report()`'s bare (year-less) call used to route to a single Chroma query pooling all requested `report_id`s into one ranked list -- the same crowding shape Decision 1 already rejected as the default for years, just never extended to reports. Post-completion analysis (`/opsx:explore`, 2026-08-31) found this reproduced Decision 1's exact failure on the report axis: `gq-022`'s combined-call report coverage looked like the worst case in the table above (1/3), but resolves fully once retrieval fans out per `report_id` the same way it already does per year -- and `gq-001`'s residual gap traced to a single year (e.g. 2022) having two requested reports (`*_annual_en`, `*_financials_en`) sharing one pooled query's slots, where the annual report swept all of them. `retrieve_by_report()` now fans out per `report_id` by default (closing this gap); the prior pooled behavior survives as the separately-named `retrieve_with_report_filter()`, for callers where `report_ids` is a scoping filter rather than a required-coverage set -- the two are identical for a single `report_id`.

| id | requested report_ids | `retrieve_by_report` (fan-out, default) | `retrieve_with_report_filter` (pooled) |
|---|---|---|---|
| gq-001 | 7 | 7/7 | 5/7 (missing ['2022_financials_en', '2023_financials_en']) |
| gq-004 | 2 | 2/2 | 2/2 |
| gq-007 | 4 | 4/4 | 3/4 (missing ['2023_annual_en']) |
| gq-010 | 2 | 2/2 | 2/2 |
| gq-012 | 2 | 2/2 | 2/2 |
| gq-014 | 2 | 2/2 | 2/2 |
| gq-017 | 3 | 3/3 | 3/3 |
| gq-022 | 3 | 3/3 | 1/3 (missing ['2022_annual_en', '2023_annual_en']) |
| gq-025 | 2 | 2/2 | 2/2 |

**Summary:** 9/9 multi-report items get full coverage under fan-out (the shipped default) vs. 6/9 under the old pooled behavior, for the identical bare (year-less) call. Items requesting only one `report_id` are excluded from this table -- the two functions are equivalent there by construction.

### `year_collision` items (gq-019, gq-020)

Per tasks.md 4.2: for these items, zero coverage on the *comparative* (non-native) year in the per-year fan-out is EXPECTED, not a bug -- `year` metadata is report-level (every chunk in e.g. `2021_financials_en` is tagged `year="2021"`), so the comparative column's year (e.g. 2020 inside the 2021 financials PDF) has no same-report, same-year-tagged chunk for fan-out to find. The real check for this failure mode is task 4.3's section-filter comparison below.

| id | requested years (native / comparative) | fan-out coverage | expected-zero? | unfiltered top hit | section-filtered top hit | collision reproduced (unfiltered) | collision avoided (filtered) |
|---|---|---|---|---|---|---|---|
| gq-019 | [2020, 2021] | 1/2 (missing [2020]) | yes | 17. Comparative figures (note) | Halifax Port Authority Consolidated statement of earnings (statement) | True | True |
| gq-020 | [2022, 2023] | 1/2 (missing [2022]) | yes | 16. Comparative figures (note) | Halifax Port Authority Consolidated statement of earnings (statement) | True | True |

**Summary:** section-filtered retrieval avoided the known footnote collision on both `year_collision` items. This directly answers design.md's open question about whether the section-type heuristic needs report-type-specific tuning for the v1 8-report corpus -- see per-item results above.

## Task 4.4 -- input to `langgraph-plan-critique-loop`

- Per-year fan-out (`retrieve_by_year`) closes gq-001-style year-crowding gaps directly: see the full-year-coverage count above. Where an item still shows missing years after fan-out, that's a genuine no-content or embedding-relevance gap (not a ranking artifact fan-out could fix), and is a candidate planner-loop retry/decompose target.
- Section-type filtering (`retrieve_with_section_filter`) directly fixes the `year_collision` footnote-vs-statement ranking failure documented in `evals/phase1_baseline.md` -- see the collision table above. This is a re-ranking signal (Decision 3), not a hard filter, so it should be exposed to the planner/critic loop as a *preference*, not a guarantee.
- The classifier's known under-triggering on unnumbered note *subsection* headings (see 3.3 above) is worth revisiting if a future golden item's collision involves an unnumbered footnote heading rather than a numbered one -- not needed for gq-019/gq-020 as currently written.
- **[2026-08-31]** Report-axis fan-out (design.md Decision 7, tasks.md 6.1-6.5) is now implemented and reflected in this run's numbers -- `retrieve_by_report()`'s bare call guarantees per-report_id coverage the same way `retrieve_by_year()` already guarantees per-year coverage. `langgraph-plan-critique-loop/design.md`'s Decision 4 entity-only dispatch (bare `retrieve_by_report()`, no year) can now be built against this default directly -- see the report-axis comparison table above for what the previous pooled default would have gotten instead.
